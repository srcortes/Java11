package ACCOUNTMAP;

public class SendMoneyService implements SendMoneyServiceUseCase{

    AcountState acountState;

    @Override
    public Account createAccount(Account account) {

        //Account account1 = acountState.createAccount(account.getIdAccount());
        //account1.validateAccount(account.getIdAccount());
        return null;
    }
}
